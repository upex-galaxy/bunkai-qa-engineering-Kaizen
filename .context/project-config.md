# Project Config — Bunkai TMS

> Generated by `/project-discovery` Phase 1 (Constitution) on 2026-05-23. Authoritative project metadata. Updated by re-running Phase 1 — never hand-edit unless `/project-discovery` is broken for your case.

---

## Repositories

| Role                    | Path                                                                     | Type                                                                         |
| ----------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------- |
| Target (app under test) | `c:\Users\teste\Desktop\projects\upex\upex-bunkai-tms`                   | Monorepo (single Next.js fullstack workspace `aicode-starter` in `bun.lock`) |
| QA Framework            | `c:\Users\teste\Desktop\projects\upex\bunkai-qa-engineering-kaizen` (this repo) | Boilerplate (KATA + Playwright + Allure + Xray)                              |

Target is a sibling directory under `c:\Users\teste\Desktop\projects\upex\`. Discovery is read-only on the target; all writes land in this framework's `.context/` and `.session/`.

> Path corrected 2026-09-20 after a machine migration (macOS → Windows); the layout and repo identities are unchanged.

---

## Project Identity

| Field                | Value                                       | Source                                               |
| -------------------- | ------------------------------------------- | ---------------------------------------------------- |
| `project_name`       | Bunkai                                      | `.agents/project.yaml > project.project_name`        |
| `project_key` (Jira) | `BK`                                        | `.agents/project.yaml > project.project_key`         |
| `webapp_domain`      | `upexbunkai.vercel.app`                     | `.agents/project.yaml > project.webapp_domain`       |
| Atlassian site       | `https://jira.upexgalaxy.com.net/`          | `.agents/project.yaml > issue_tracker.atlassian_url` |
| Issue tracker        | Jira (CLI: `acli`)                          | `.agents/project.yaml > issue_tracker`               |
| TMS                  | Xray on Jira (CLI: `bun xray`) — Modality A | `.agents/project.yaml > testing.tms_cli`             |
| Default test env     | `staging`                                   | `.agents/project.yaml > testing.default_env`         |

> `kata-manifest.json` (framework) is the source of truth for KATA Components and ATC IDs. Per Critical Rule #12, always validate against it before proposing new test artifacts.

---

## Tech Stack (target `upex-bunkai-tms`)

| Layer           | Tech                                                                                   | Version                       | Source                                                                         |
| --------------- | -------------------------------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------ |
| Runtime         | Bun                                                                                    | `>= 1.0`                      | `bun.lock`, `package.json > engines` (implicit via `bun cli/...` scripts)      |
| Language        | TypeScript                                                                             | `^5.9.3`                      | `bun.lock > devDependencies`                                                   |
| Framework       | Next.js                                                                                | `^15` (App Router)            | `bun.lock > dependencies.next`; `next.config.ts` present                       |
| UI              | React                                                                                  | `^19` + React DOM `^19`       | `bun.lock > dependencies`                                                      |
| UI primitives   | Radix UI (`@radix-ui/react-{dialog,dropdown-menu,tabs,tooltip}`)                       | `1.1.x` / `1.2.x` / `2.1.x`   | `bun.lock`                                                                     |
| UI utilities    | `clsx`, `class-variance-authority`, `tailwind-merge`, `cmdk`, `lucide-react`, `sonner` | latest                        | `bun.lock`                                                                     |
| Styles          | Tailwind CSS                                                                           | `^3.4`                        | `bun.lock > devDependencies`; `app/globals.css`                                |
| Style toolchain | PostCSS + Autoprefixer                                                                 | `^8.5` / `^10.5`              | `bun.lock > devDependencies`                                                   |
| DB / Auth       | Supabase (`@supabase/ssr`, `@supabase/supabase-js`)                                    | `^0.10.3` / `^2.106.0`        | `bun.lock > dependencies`                                                      |
| Database engine | Supabase Postgres (managed)                                                            | —                             | `.agents/project.yaml > database.db_type`; `supabase/migrations/0001…0008.sql` |
| Validation      | Zod                                                                                    | `^4.4.3`                      | `bun.lock`                                                                     |
| API contract    | `@asteasolutions/zod-to-openapi` (codegen)                                             | `^8.5.0`                      | `bun.lock` + `scripts/openapi-gen.ts`, `scripts/openapi-diff.ts`               |
| API docs        | `@scalar/api-reference-react`                                                          | `^0.9.38`                     | `bun.lock`; rendered at `/api/docs` route                                      |
| Tables          | `@tanstack/react-table`                                                                | `^8.21.3`                     | `bun.lock`                                                                     |
| Code editor     | `@monaco-editor/react`                                                                 | `^4.7.0`                      | `bun.lock` (presumed for ATC step authoring UX)                                |
| Lint            | ESLint 9 + `@antfu/eslint-config` + `@next/eslint-plugin-next`                         | `^9.28` / `^4.16` / `^16.2.6` | `bun.lock`                                                                     |
| Format          | Prettier                                                                               | `^3.7.4`                      | `bun.lock`                                                                     |
| Hooks           | Husky + lint-staged                                                                    | `^9.1.7` / `^16.2.7`          | `bun.lock`; `.husky/` present in target                                        |

> **Source-of-truth note**: the target's `package.json` is OUT OF SYNC with `bun.lock` — it still shows the boilerplate fingerprint (`agentic-dev-boilerplate`, CLI-only deps). The lock declares the real workspace name (`aicode-starter`) and Next/Supabase deps. See Discovery Gaps below.

---

## OpenAPI / API Contract Source

- **Spec file**: `<target>/public/openapi.json` (generated, served as static asset).
- **Generator**: `<target>/scripts/openapi-gen.ts` (consumes Zod schemas via `@asteasolutions/zod-to-openapi`).
- **Diff tool**: `<target>/scripts/openapi-diff.ts`.
- **Live endpoint**: `GET /api/openapi` (Next.js route handler in `<target>/app/api/openapi/route.ts`).
- **Docs UI**: `<target>/app/api/docs/page.tsx` (renders via `@scalar/api-reference-react`).
- **Framework sync**: this framework's `bun run api:sync` (consumes the target spec → `api/openapi-types.ts`). Will be invoked by `/adapt-framework` after discovery is approved.

---

## Environments

| Env                                      | Web URL                                 | API URL                                     | DB MCP          | API MCP           | Notes                                                                                    |
| ---------------------------------------- | --------------------------------------- | ------------------------------------------- | --------------- | ----------------- | ---------------------------------------------------------------------------------------- |
| `local`                                  | `http://localhost:3000`                 | `http://localhost:3000/api`                 | `local-dbhub`   | `local-openapi`   | Requires target `bun install` after fixing `package.json` desync; then `bun dev`.        |
| `staging` (default)                      | `https://staging-upexbunkai.vercel.app` | `https://staging-upexbunkai.vercel.app/api` | `staging-dbhub` | `staging-openapi` | Vercel preview/staging deploy (presumed auto from Git).                                  |
| `production` (not in `project.yaml` yet) | `https://upexbunkai.vercel.app`         | `https://upexbunkai.vercel.app/api`         | —               | —                 | Implied from `project.webapp_domain`; add to `.agents/project.yaml` before testing prod. |

Source: `.agents/project.yaml > environments`. Active env defaults to `staging` per `testing.default_env`.

---

## Credentials

- All secrets live in `<framework>/.env` (NOT in this file). See `<framework>/.env.example` for the key list.
- Atlassian creds present: `ATLASSIAN_URL`, `ATLASSIAN_EMAIL`, `ATLASSIAN_API_TOKEN` (validated 2026-05-23 — duplicated lines, last value wins; recommend cleanup).
- Supabase keys for target (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_PROJECT_REF`) — referenced in target's `.env.example`. Confirm present in framework's `.env` before Phase 3 deep-run.
- Test users (`STAGING_USER_EMAIL`, `STAGING_USER_PASSWORD`, etc.): **Discovery Gap** — not yet provisioned in target app. See Discovery Gaps.

---

## Team Contacts

**Discovery Gap** — not yet collected. To fill: ask the user for:
- Product Owner (PO) — name + Jira handle
- Tech Lead (TL) — name + Slack/email
- QA Lead (QL) — name + Slack/email
- On-call / Incident contact

---

## Discovery Gaps

The following items could not be verified from code and require human confirmation:

- [ ] **Team contacts** — PO / TL / QA Lead / on-call (see above).
- [ ] **`package.json` ↔ `bun.lock` desync in target** — `package.json` shows `agentic-dev-boilerplate` (CLI tooling only) but `bun.lock` workspace is `aicode-starter` with `next ^15`, `react ^19`, `@supabase/*`, Radix UI, Tailwind, zod, etc. `node_modules/next` and `node_modules/@supabase` are missing → the app cannot run locally until `package.json` is synced (or `bun install --force` against the lock resolves cleanly). Confirm with the user whether (a) the target intentionally tracks deps only in the lock, (b) `package.json` should be regenerated to match the lock, or (c) the app is pre-production and `bun install` has not been run since extending the boilerplate.
- [ ] **Production env in `.agents/project.yaml`** — `production` environment block missing; `webapp_domain` implies it but `environments` only has `local` + `staging`. Add a `production` block before running tests against prod.
- [ ] **Test users in Supabase Auth** — not yet created in the app. Required before Phase 3 can capture real auth requests; see `.context/infrastructure/backend.md` Phase 3 auth section once generated.
- [ ] **CI/CD config** — no `.github/workflows/` in target. Vercel deploy presumed auto-via-Git; confirm + document where build/test jobs live (or that they don't exist yet).
- [ ] **Atlassian `.env` duplicates** — `ATLASSIAN_URL`, `ATLASSIAN_EMAIL`, `ATLASSIAN_API_TOKEN` appear twice each in framework `.env`. Last value wins; cleanup recommended (cosmetic, non-blocker).
- [ ] **Monitoring / observability** — no Sentry, Datadog, New Relic, or similar detected in target deps. Confirm whether observability is intentionally absent or hosted off-repo (e.g., Vercel Analytics, Supabase logs).
