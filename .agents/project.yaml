# Project configuration consumed by AI agents (Claude, Cursor, Gemini, Codex, etc.)
# when they encounter {{VAR_NAME}} references in skills, commands, templates and docs.
# Variable names are snake_case; the AI maps {{PROJECT_NAME}} -> project.project_name lexically.
# Edit values manually, or run `bun run agents:setup` for an interactive walkthrough.
# Every unfilled field is `null` plus a TODO comment with a concrete example.

project:
  project_name: Bunkai # Project name (e.g. MyProject)
  project_key: BK # Project key in issue tracker (e.g. PROJ, ACME, UPEX). The boilerplate ships this `null` on purpose — the consumer project fills it; never hardcode a real key here.
  webapp_domain: upexbunkai.vercel.app # Domain of the web application under test (e.g. myproject.com)

backend:
  backend_repo: ../upex-bunkai-tms # Relative path to backend repository (e.g. ../my-backend)
  backend_stack: Next.js + Supabase + Vercel # Backend technology stack (e.g. Node.js + Express)
  backend_entry: ../upex-bunkai-tms/. # Backend source entry point (e.g. src/)

frontend:
  frontend_repo: ../upex-bunkai-tms # Relative path to frontend repository (e.g. ../my-frontend)
  frontend_stack: Next.js # Frontend technology stack (e.g. React + TypeScript)
  frontend_entry: ../upex-bunkai-tms/. # Frontend source entry point (e.g. src/)

database:
  db_type: Supabase Postgres # Database engine (e.g. PostgreSQL)

issue_tracker:
  issue_tracker: Jira # Issue tracking tool (e.g. Jira)
  issue_tracker_cli: acli # CLI command to query tickets (e.g. acli (Jira) / gh issue (GitHub))
  atlassian_url: 'https://upexgalaxy72.atlassian.net' # Atlassian site base URL (e.g. https://company.atlassian.net) — the ONLY place the host lives; read it with `bun run --silent jira:url`

testing:
  default_env: staging # Default testing environment — must match a key under `environments:` (e.g. staging)
  tms_cli: bun xray # Test management CLI command (e.g. bun xray)
  tc_creation_stage: auto # Which stage creates Test work items — see sprint-testing/SKILL.md §"Which stage creates the TCs"

qa:
  formal_blocked_gate: true # When true and the substrate exposes a `blocked` story status, FAILED stories dispatch `defect_reported` (in_test -> blocked). When false (or no `blocked` slug), the skill leaves the story in `in_test` with the linked bug — non-strict fallback. See sprint-testing/references/sprint-orchestration.md Briefing 4 Step 5.
  # QA process epics — buckets that hold QA artifacts so they never pollute product epics/roadmap.
  # Bug/Defect/Improvement parent to `defect_epic`; Test (TC) issues parent to `test_repository_epic`.
  # Skills resolve the epic by NAME (the methodology convention shipped below) and find-or-create it,
  # caching the discovered `*.key` here. NEVER parent a quality issue to a product/dev epic.
  # Read directly by skills (not {{VAR}} tokens) → skipped by vars:check, like git_strategy leaves.
  # Doctrine: .agents/skills/agentic-qa-core/references/defect-management-doctrine.md (Part 4).
  qa_artifact_label: QA-Artifact # Label stamped on every QA process epic; sync-jira-issues.ts reads it to index them under qa-artifacts/ instead of .context/PBI/epics/
  qa_epics:
    defect_epic:
      name: QA Defect Management # convention name; the `QA ` prefix marks a process epic, not product
      key: null # discovered/created at runtime, then cached here (e.g. PROJ-123)
    test_repository_epic:
      name: QA Test Repository # convention name; holds every Test (TC) issue
      key: null # discovered/created at runtime, then cached here (e.g. PROJ-456)
    master_test_plan_epic:
      name: QA Master Test Plan # convention name; holds every Test Plan (FTP/STP/ATP)
      key: null # discovered/created at runtime, then cached here (e.g. PROJ-789)
    test_artifacts_epic:
      name: QA Test Artifacts # convention name; holds every Test Execution (STR/ATR), Precondition and Test Set
      key: null # discovered/created at runtime, then cached here (e.g. PROJ-790)

# Per-environment config. Add/remove environments freely (production, dev, qa, uat, etc.).
# Each environment must define ALL four keys: web_url, api_url, db_mcp, api_mcp.
# Skills referencing {{WEB_URL}} / {{API_URL}} / {{DB_MCP}} / {{API_MCP}} resolve to the
# active environment (`testing.default_env` above, or a session override).
# For explicit cross-env references use {{environments.<env>.<var>}} (e.g. {{environments.local.web_url}}).
environments:
  local:
    web_url: 'http://localhost:3000' # Frontend URL for local env (e.g. http://localhost:3000)
    api_url: 'http://localhost:3000/api' # API base URL for local env (e.g. http://localhost:3000/api)
    db_mcp: local-dbhub # MCP server name for local DB (e.g. myproject-local-db)
    api_mcp: local-claude # MCP server name for local API (e.g. myproject-local-api)
  staging:
    web_url: 'https://staging-upexbunkai.vercel.app' # Frontend URL for staging env (e.g. https://staging.myproject.com)
    api_url: 'https://staging-upexbunkai.vercel.app/api' # API base URL for staging env (e.g. https://api-staging.myproject.com)
    db_mcp: staging-dbhub # MCP server name for staging DB (e.g. myproject-staging-db)
    api_mcp: staging-claude # MCP server name for staging API (e.g. myproject-staging-api)
git_strategy:
  strategy: sdet # null until Strategy Setup runs; then one of: solo-main | main-integration | enterprise | trunk-based | gitflow | github-flow | gitlab-flow | sdet
  description: >
    SDET integration-trunk flow for chained test-automation suites. main holds confirmed
    tests (protected by rulesets + required checks). Each suite spins up an ephemeral
    integration trunk test/<module>-suite cut off main; every ticket is cut FROM the trunk,
    validated locally on local AND staging, PR'd INTO the trunk, and merged with --no-ff
    (never squash) to preserve per-ticket history. A sync gate (git merge origin/main) runs
    on the trunk before a single final trunk → main PR — the only PR that faces main's
    rulesets. Trunk is deleted after the final PR merges; the next suite starts fresh.
    Adjacent non-test work rides Plus Branches (docs/chore/fix) into the trunk. Full runbook:
    .claude/skills/git-flow-master/references/sdet-integration-trunk.md.
  branches:
    production: main # release / default branch — holds confirmed tests
    integration: null # no long-lived integration branch (sdet trunk is ephemeral, not long-lived)
    ephemeral_pattern: test/<module>-suite # on-demand per-suite integration trunk, created off main by the Branch operation
  protected: [main] # branches requiring explicit confirm before a direct push
  decisions: # captured by the Strategy Setup questionnaire (Q1/Q2/Q3) — sdet pre-answers Q1/Q3 as n/a, Q2 fixed at merge-commit
    promote_method: n/a # ff-only | merge-commit | squash | n/a — n/a: final trunk → main PR follows the repo's allowed merge method
    feature_merge: merge-commit # merge-commit | squash | rebase-merge | n/a — FIXED: --no-ff preserves per-ticket history (defining property of sdet)
    hotfix_policy: n/a # branch-off-prod-backmerge | via-integration | none | n/a
  policy: # how strictly protected branches are guarded (set by Strategy Setup Q4)
    direct_push_to_protected: confirm # forbidden | confirm | allowed (sdet = confirm — trunk self-merged by maintainer; main takes only the final suite PR)
    admin_bypass: true # team POLICY: may a repo admin bypass PR/protection for urgent changes? actual capability still depends on the GitHub user's role — the skill re-confirms at runtime
    require_pr_reviews: 1 # null | 0 | N — VERIFIED 2026-09-04 against ruleset ProtectPublic (id 16809542): required_approving_review_count = 1 on the final trunk → main PR
    # Host divergences that are ACCEPTED, not drift. Each `field` names a `git:policy verify`
    # finding verbatim (`<branch>.<policy_field>`); verify moves matching findings from DRIFT
    # to ACCEPTED (exit 0) and warns when an entry no longer matches anything (stale entry).
    accepted_divergences:
      - field: main.require_code_owner_review
        enforced: true
        accepted: 2026-09-04
        reason: >
          Enforced by the ProtectPublic ruleset with no CODEOWNERS file in this repo, so
          nobody outside the bypass list could ever satisfy it. Not adding a CODEOWNERS file
          and not applying the yaml to the host: the maintainer's admin credential merges
          the final trunk -> main PR via the ruleset's bypass list (standing authorization —
          see git_strategy.description). The host rule keeps protecting every non-bypass
          contributor.
  branch_prefixes:
    precedence: [feat, fix, refactor, test, docs, chore]
    naming_with_key: '{prefix}/{ISSUE-KEY}-{kebab-slug}'
    naming_without_key: '{prefix}/{kebab-slug}'
  meta:
    setup_version: 2
    created: 2026-06-29 # YYYY-MM-DD stamped by Strategy Setup
    strategy_source: chosen # inherited | chosen — CHOSEN 2026-06-29 via git-flow-master Strategy Setup (sdet, for chained test-automation suites); not the shipped default
    policy_verified: 2026-09-04 # YYYY-MM-DD of the last `bun run git:policy verify`. null = never reconciled against the host
    policy_source: accepted # verified | accepted | declared — every divergence is formally listed in policy.accepted_divergences

# Files `bun run up` must keep as THIS project's own. Read by `cli/update-boilerplate.ts`
# at the start of every run and merged into the protected watchlist (AGENTS.md,
# .agents/project.yaml, .mcp.json, opencode.jsonc, .codex/config.toml,
# .claude/settings.json, .husky/pre-commit, .husky/pre-push, allurerc.mjs,
# playwright.config.ts, the KATA bases under tests/components/, ...): a listed path is
# never overwritten, is delivered once from upstream when missing, and shows a drift row
# with hunk evidence when upstream's copy changes. List a SYNCED file you merged by hand
# (the parity row "project edit overwritten" names the path to add); repo-relative FILE
# paths only, no directories, nothing under .git. An invalid entry is reported and
# ignored. Empty by default. Read directly by the updater (not a {{VAR}} source): the
# nested list is skipped by vars:check like git_strategy and qa.qa_epics.
updater:
  protected_paths: [scripts/lint-skills.ts] # kept: adds committed-community-skill handling on top of upstream's tier logic — see the STALE_PATH_ALLOWED note in the file
